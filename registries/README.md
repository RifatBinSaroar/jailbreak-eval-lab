# The canonical research records

All nine JSON files share `src/jailbreak_eval/schemas/registry.schema.json`.
Keep earlier revisions. Append a new version and timestamp under the same stable ID.

```text
python scripts/research.py validate
python scripts/research.py build-web
```

See [the easy guide](../docs/PLATFORM_QUICKSTART.md) and [contract details](../docs/REGISTRIES.md).
The initial reading/planning entries are not verified findings. Experiments and results remain zero.
